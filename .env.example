# Public build-time values only. Never put private API keys or passwords here.
# Astro exposes PUBLIC_* values to the browser, so treat them as public.
PUBLIC_KIT_FORM_ACTION=
PUBLIC_GA_MEASUREMENT_ID=
